# Here are your Instructions
To run this application in developer mode run the following command: "npm install" & "npm start"
It needs the node version 18 to install it do "nvm install 18" "nvm use 18"

